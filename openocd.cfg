set ESP_RTOS hwthread
set ESP_ONLYCPU 1
source [find board/esp32s3-builtin.cfg]

# Reset and halt the target the moment GDB connects so that VS Code
# can inject editor breakpoints before the program starts running.
esp32s3 configure -event gdb-attach {
    reset halt
}

